server:
  port: 9090
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: user-service
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    password: root
    url: jdbc:mysql://127.0.0.1:3306/useunicode?useUnicode=true&characterEncoding=utf8
    username: root
mybatis:
  mapperLocations: classpath:mapper/*.xml
  typeAliasesPackage: com.zy.uua.pojo
feign:
  hystrix:
    enabled: true
